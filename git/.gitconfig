# Identity and per-machine includes live in ~/.gitconfig.local (not versioned).
[include]
	path = ~/.gitconfig.local
[filter "lfs"]
	process = git-lfs filter-process
	required = true
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
